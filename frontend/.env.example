# Target URL for the Vite proxy (used during development)
VITE_API_TARGET_URL=http://127.0.0.1:8000

# Base URL for API calls (leave empty to use relative paths, which will go through proxy in dev, or set to the backend URL for production)
VITE_API_BASE_URL=
